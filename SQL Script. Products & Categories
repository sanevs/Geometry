SELECT products.name, categories.name FROM products 
INNER JOIN productsCategories ON products.id = productsCategories.productId
LEFT JOIN categories ON productsCategories.categoryId = categories.id;
